#include <bits/stdc++.h>
#define ll long long int
using namespace std;


int clc(int a[],int cond)

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, m;
    cin >> n >> m;
    int a[n];
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }

}
