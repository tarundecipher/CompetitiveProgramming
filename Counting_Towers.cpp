#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int clc(int a,){
    if(a==0){
        return 1;
    }
    if(a<0){
        return 0;
    }
    int a1 = 0;
    for(int i=1;i<=a;i++){
        a1+=
    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--)
    {
        int n; cin>>n;

    }
}
